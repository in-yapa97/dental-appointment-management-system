# -----------------------------------------------------------------------------
# Frontend Environment Configuration Template
# Copy this file to .env.local (do NOT commit .env.local with real production URLs)
# -----------------------------------------------------------------------------

# Production Backend API URL
# Leave empty or set to http://localhost:8080 for local development (uses Vite dev proxy)
VITE_API_BASE_URL=http://localhost:8080
